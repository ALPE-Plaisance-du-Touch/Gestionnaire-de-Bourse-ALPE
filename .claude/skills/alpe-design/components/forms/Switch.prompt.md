Interrupteur ALPE — réglage appliqué immédiatement. Pour un choix à valider dans un formulaire, préférer `Checkbox`.

```jsx
<Switch label="Recevoir la lettre d'information" checked={on} onChange={setOn} />
```
