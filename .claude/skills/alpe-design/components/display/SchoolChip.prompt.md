Établissement scolaire — brique de la page « Les établissements » et du menu.

```jsx
<SchoolChip name="Galilée" level="college" city="La Salvetat" href="#" />
```

Niveaux : `maternelle` (orange), `elementaire` (bleu), `college` (jaune), `lycee` (gris).
