# =============================================================================
# Production Environment Variables - Bourse ALPE
# =============================================================================
# Copy this file to .env.production and fill in all values.
# NEVER commit .env.production to git.
# =============================================================================

# --- Domain ---
DOMAIN=bourse.alpe-plaisance.org
ADMIN_EMAIL=admin@alpe-plaisance.org

# --- Database ---
DB_ROOT_PASSWORD=CHANGE_ME_strong_root_password
DB_NAME=bourse_alpe
DB_USER=bourse
DB_PASSWORD=CHANGE_ME_strong_db_password

# --- Backend ---
APP_ENV=production
DEBUG=false
JWT_SECRET_KEY=CHANGE_ME_generate_with_openssl_rand_hex_32
FRONTEND_URL=https://bourse.alpe-plaisance.org
CORS_ORIGINS=https://bourse.alpe-plaisance.org

# --- SMTP (production mail server) ---
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USE_TLS=true
SMTP_USER=noreply@alpe-plaisance.org
SMTP_PASSWORD=CHANGE_ME_smtp_password
SMTP_FROM=noreply@alpe-plaisance.org
